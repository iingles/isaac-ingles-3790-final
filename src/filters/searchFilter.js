search: { filter: null, text: "" }

//filter for search

export const searchFilter = (filterText, searchString) => {
    const search = {
        filter: null,
        text: searchString,
    }

    // return this.search.text.filter( (this.search.filter) => {
    //     return element.match(this.filterText);
    // })

}